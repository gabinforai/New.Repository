// 백엔드 API 기본 주소
// - 백엔드 서버가 프론트엔드 파일도 함께 제공하는 경우(같은 서버, 같은 주소)에는
//   상대 경로 '/api'만으로 충분합니다. 지금 기본 설정이 이 방식입니다.
// - 프론트엔드를 백엔드와 다른 주소/포트에서 따로 띄운다면 아래 값을
//   백엔드의 실제 주소로 바꿔주세요.
//   예: const API_BASE_URL = 'http://localhost:3000/api';
const API_BASE_URL = '/api';
